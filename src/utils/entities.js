export const types = {
  all: {},
  customers: { customer: {} },
  materialProviders: { provider: { provider_materials: {} } },
  serviceProviders: { provider: { provider_services: {} } },
  workerProviders: { provider: { provider_workers: {} } }
};
